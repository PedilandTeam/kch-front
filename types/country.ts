

export namespace CountryNamespace {
    export type GET= {
        id: number,
        name: string,
        code: string,
        pageCount: number
        status: boolean
        availability: boolean
        areaCode: number
        englishName: string
    }
}